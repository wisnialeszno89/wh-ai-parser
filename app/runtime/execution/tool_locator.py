class ToolLocator:

    def locate(
        self,
        tool,
    ):

        print(
            f"[LOCATE] {tool.name}"
        )

        #
        # później:
        #
        # Vision
        # GUI Memory
        # AI
        #

        return {
            "tool": tool.name,
            "x": 0,
            "y": 0,
        }